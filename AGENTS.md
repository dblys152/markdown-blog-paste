# MD2Blog Agent Guidelines

이 파일은 MD2Blog를 변경할 때 따라야 할 작업 원칙을 정의한다. 제품 정책과 구체적인
아키텍처 결정은 `docs/product-architecture.md`를 기준으로 한다. 브라우저와 외부
에디터에서 확인할 사항은 `docs/manual-regression-checklist.md`를 따른다.

## 1. 판단 순서

현재 배포 환경이나 사용 중인 외부 서비스의 편의성을 도메인·애플리케이션
아키텍처의 기준으로 삼지 않는다.

```text
비즈니스 정책과 문제 정의
-> 불변식과 신뢰성 수준 정의
-> 도메인과 애플리케이션 경계 정의
-> 기술 중립적인 Port 정의
-> 인프라 Adapter 선택
-> 배포 환경 연결
```

Cloud Run, Cloud Logging, Cloud Scheduler, Gmail SMTP, Neon은 확정된 요구사항을
구현하는 인프라 선택이다. 해당 제품을 사용한다는 사실로 도메인 정책을 역산하지
않는다.

- 배포 대상이 바뀌어도 도메인과 애플리케이션 정책은 유지되어야 한다.
- 외부 제품은 Port 뒤의 Adapter로 격리한다.
- 정책이 논의 중이면 현재 인프라에 맞춘 해법을 먼저 확정하지 않는다.
- 기술을 제안할 때 해결할 요구사항과 아키텍처 결정·인프라 선택을 구분한다.
- 확정된 문서와 코드가 다르면 차이를 먼저 보고하고 임의로 한쪽을 기준 삼지 않는다.

## 2. 설계 검토

기능이나 모델을 변경하기 전에 다음을 확인한다.

1. 데이터가 현재 상태인지 변경 이력인지 구분했는가?
2. 엔터티의 생성·갱신·삭제 시점과 소유자가 명확한가?
3. 관계가 `1:1`, `1:0..1`, `1:N` 중 무엇인지 정의했는가?
4. 동시 요청에서도 DB가 불변식을 보장하는가?
5. 성공·실패·재시도·만료·철회 상태가 구분되는가?
6. 예상 가능한 다음 정책이 기존 모델의 의미를 깨뜨리지 않는가?
7. 필요한 확장 경계와 불필요한 선행 구현을 구분했는가?

저장 행 수를 줄이기 위해 생명주기가 명확한 엔터티를 불완전하게 만들지 않는다.
반대로 미래 가능성만으로 사용되지 않는 범용 프레임워크를 만들지 않는다.

### 적용된 이력과 공개된 계약의 불변성

한 번 적용되었거나 다른 주체가 소비한 이력과 계약은 불변으로 취급한다. 이후 변경은
과거 기록을 소급 수정하지 않고 새 버전, 새 마이그레이션, 호환 계층 또는 명시적인 변환
과정으로 표현한다.

- 공유·개발·운영 DB에 적용한 마이그레이션은 기존 파일을 수정하지 않고 새 revision으로
  변경한다.
- 발행된 도메인 이벤트와 Outbox 메시지의 이름·필드·의미를 변경할 때는 기존 소비자의
  호환성을 검토하고 필요한 경우 새 버전을 정의한다.
- 공개된 API와 저장 포맷을 변경할 때는 소비자와 기존 데이터의 변환 경로를 함께
  정의한다.
- `alembic current`처럼 이력 식별자가 최신이라는 사실만으로 실제 상태가 계약과
  일치한다고 판단하지 않는다.
- 개인 작업이라도 초기화하지 않을 DB에 한 번 실행한 마이그레이션은 적용된 이력으로
  간주한다.

## 3. DDD 계층 경계

- Domain은 비즈니스 규칙과 상태 전이를 담당하며 프레임워크에 의존하지 않는다.
- Application은 유스케이스의 순서와 트랜잭션 경계를 조정한다.
- Presentation은 HTTP 입력·출력을 애플리케이션에 연결한다.
- Infrastructure는 DB, 암호화, 메일, 외부 인증 제공자의 구현을 담당한다.
- `HTTP DTO -> Command -> Domain` 흐름을 유지한다.
- DB 조회가 필요한 Command 조립은 Application Factory가 담당할 수 있다.
- 인프라 예외는 경계에서 도메인 또는 애플리케이션 오류로 변환한다.
- 저장소 인터페이스에는 컬럼 변경 플래그보다 도메인 목적이 드러나는 연산을 둔다.
- 현재 상태와 과거 이력을 같은 모델의 임시 필드로 혼합하지 않는다.

## 4. 데이터베이스와 동시성

- 선행 조회만으로 유일성을 보장하지 않고 DB 제약을 최종 방어선으로 둔다.
- 필요한 경우 `SELECT ... FOR UPDATE`, 원자적 UPDATE 또는 UPSERT를 사용한다.
- 최초 INSERT 경합, 중복 요청, 재시도를 정상적인 운영 시나리오로 취급한다.
- Aggregate와 필수 관련 상태는 같은 트랜잭션에서 저장한다.
- 외부 부작용을 DB 커밋과 같은 것으로 간주하지 않는다.
- 만료·철회 데이터에는 정리 및 보존 정책을 둔다.

마이그레이션에는 upgrade, downgrade, 기존 데이터 이전, 제약·인덱스, 배포 순서 검토를
포함한다. 로컬 또는 운영 DB에 마이그레이션을 직접 실행하기 전에는 사용자에게
요청하거나 명시적인 허가를 받는다.

## 5. 테스트와 완료 기준

- 도메인 상태 전이는 단위 테스트로 검증한다.
- 애플리케이션 서비스는 성공·실패·재시도·경계 시각을 검증한다.
- 저장소는 제약·잠금·조회 범위를 검증한다.
- API 테스트는 상태 코드와 공통 오류 응답을 검증한다.
- 동시성 불변식은 가능한 범위에서 통합 테스트를 둔다.
- 변경 완료 전 전체 테스트, Ruff, mypy, Alembic head, `git diff --check`를 확인한다.

## 6. 문서화

- `docs/product-architecture.md`에는 확정된 제품 정책, 도메인 규칙, 아키텍처 경계,
  기술적 의사결정과 트레이드오프만 기록한다.
- 구현 진행 상황, 전환 계획, 데이터 마이그레이션 절차, 배포 절차와 작업 목록을
  `docs/product-architecture.md`에 기록하지 않는다.
- 사용자에게 제공되는 현재 기능은 `README.md`에 반영한다.
- 수동 확인이 필요한 흐름은 `docs/manual-regression-checklist.md`에 반영한다.
- 논의 중인 정책과 미확정 작업은 정의 문서에 넣지 않고 이슈 또는 별도의 계획 문서에서
  관리한다.
- 데이터 변경과 배포에 필요한 실행 절차는 별도의 운영 문서에서 관리한다.
